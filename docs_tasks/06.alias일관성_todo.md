
3. Low 버전 관리 우회 가능 API가 남아 있음
   ProductIndexingService#reindexData()는 기본적으로 properties.indexName()에 바로 색인합니다.


일관되게 잘 구현된 부분

- 버전드 인덱스 생성: src/main/java/com/example/aisearch/service/indexing/domain/VersionedIndexNameGenerator.java:32
- 롤아웃 순서(신규 생성 → 색인 → alias 전환 → 구 인덱스 정리): src/main/java/com/example/aisearch/service/indexing/orchestration/IndexRolloutService.java:50
- 검색 시 기본 경로가 read alias 사용: src/main/java/com/example/aisearch/service/search/strategy/KnnSearchStrategy.java:62

오픈 포인트

- 운영 정책을 “food-products-read 강제”로 볼지, “설정 가능한 read alias”로 볼지 결정이 필요합니다.
  지금 코드는 후자(설정 유연성)입니다.

원하면 다음 턴에서 strict 정책으로 바로 정리( fallback 제거, startup validation 추가, 테스트 설정/케이스 보강 )까지 패치하겠습니다.
