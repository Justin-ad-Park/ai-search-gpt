## Archive

이 폴더는 기존 root 경로에 있던 shell 스크립트 원본을 임시 보관합니다.

- 현재 사용 경로: `sh_bin/setup`, `sh_bin/check`, `sh_bin/model`, `sh_bin/analyze`, `sh_bin/deploy`
- 보관 목적: 과거 번호 체계와 실행 흐름 참고
- 운영/문서/신규 실행은 `_archive`가 아닌 새 폴더 구조를 기준으로 합니다.
